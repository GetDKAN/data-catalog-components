import React from 'react';

import DataIcon from '../../components/DataIcon';

export default {
  title: 'Components/DataIcon',
  component: DataIcon,
};

const Template = (args) => <DataIcon {...args} />;

export const Default = Template.bind({});
Default.args = {};
