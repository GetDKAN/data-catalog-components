import React from 'react';

import SearchContent from '../../templates/SearchContent';

export default {
  title: 'WIP/SearchContent',
  component: SearchContent,
};

const Template = (args) => <SearchContent {...args} />;

export const Default = Template.bind({});
Default.args = {};
