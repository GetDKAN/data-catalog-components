---
name: Header
menu: Templates
route: /templates/header
---

import { Playground, Props } from 'docz'
import Header from './index'
import menu from '../../examples/menu.json'

# Header

## Properties

<Props of={Header} />

## Basic usage

<Playground>
  <Header navItems={
    menu.map((item) => (<a href={item.url}>{item.label}</a>))}
    site="Site Name"
  />
</Playground>
