---
name: Datatable Page Sizer
menu: Components
route: /components/datatable-page-sizer
---

import { Playground, Props } from 'docz'
import DataTablePageSizer from './index'

# Datatable Page Sizer
This component allows the user to select how many rows of data to display at a time. The max value is 100 to keep large data files from crashing the website.

## Properties

<Props of={DataTablePageSizer} />

## Basic usage

<Playground>
  <DataTablePageSizer
    pageSizeChange={() => ({})}
    id="123"
  />
</Playground>
