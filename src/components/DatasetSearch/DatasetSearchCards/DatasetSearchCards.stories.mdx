import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import DatasetSearchCards from './';

<Meta title="Components/Dataset Search/Dataset Search Cards" component={DatasetSearchCards} />

# Search List Item

export const Template = (args) => <DatasetSearchCards {...args} />

<Canvas>
  <Story name="Basic">
    {Template.bind({})}
  </Story>
</Canvas>
